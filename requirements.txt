# setuptools>=65 breaks building gym==0.21 (SB3 1.8 pins it). Use install_deps.ps1 / install_deps.sh on Windows/macOS/Linux.
setuptools>=59.5,<65.0
wheel
PyYAML>=6.0
python-dotenv>=1.0.0
# Upper bound <1.25: common Anaconda numba (e.g. 0.57) requires numpy<1.25; SB3 1.x still fine on 1.24.x
numpy>=1.23.5,<1.25
stable-baselines3>=1.8.0,<2.0
sb3-contrib==1.8.0
https://github.com/andreped/sapai-gym/releases/download/v0.1.1/sapai_gym-0.1.1-py3-none-any.whl
keyboard
pyautogui
tqdm
opencv-python
scikit-image
# 1.2+ has wheels on modern Python; <1.4 keeps OneHotEncoder(..., sparse=) used by sapai-gym 0.1.1 (removed in sklearn 1.4)
scikit-learn>=1.2.2,<1.4.0
pynput